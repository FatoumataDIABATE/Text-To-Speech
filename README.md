Pour lancer le projet en local, vous devez créez un fichier nommé ```config.js``` à la racine du projet, son contenu sera :```export const API_CONFIG = {
    BASE_URL: 'URL',
    API_KEY: 'clé de l'API'
};```
